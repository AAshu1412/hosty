# templates/pv.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: hosty-postgres-pv
spec:
  capacity:
    storage: {{ .Values.storage.postgresSize }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/k8s-data/postgres # Physical folder on EC2